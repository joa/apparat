package apparat.foo {
	/**
	 * The Bar class is the philosopher's stone.
	 *
	 * The holy grail of programming. We could not exist without the foo and the bar.
	 * It releates to Jing and Jang. It is Black and White. The Good and the Evil.
	 *
	 * @author Joa Ebert
	 */
	public final class Bar {
		/**
		 * Creates and returns a new Bar object.
		 */
		public function Bar() {
		}


		/**
		 * Generates and returns the string representation
		 * of the current object.
		 *
		 * @return The string representation of the current object.
		 */
		public function toString(): String {
			return '[Bar]';
		}
	}
}